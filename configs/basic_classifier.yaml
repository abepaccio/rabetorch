BASE:
  - models/backbone/basic_vgg.yaml
  - models/head/basic_linear.yaml
  - solvers/basic_classifier.yaml
  - dataset/basic_classifier.yaml

SOLVER:
  BASE_LR: 0.001
  MAX_EPOCH: 10

DATA:
  TOTAL_BATCH: 64
  NUM_GPU: 1

EVALUATION:
  TYPE: BasicACC

BASE_CONFIG: basic_classifier.yaml
